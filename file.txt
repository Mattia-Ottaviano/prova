installazioni: pip install Flask-PyMongo
               pip install Flask
               pip install dnspython # per risolvere l'indirizzo del db
               npm install -g --force nodemon
               pip install flask-cors
               npm i -g nodemon
               
        
avvio applicazione python: nodemon main.py

installazione maps: npm install @angular/google-maps

link MongoDB: https://cloud.mongodb.com/user